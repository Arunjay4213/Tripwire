# tripwire — direct dependencies.
#
# pyproject.toml is the CANONICAL, authoritative dependency source (used by the
# package and CI: `pip install -e ".[dev]"`). This file is a convenience: a
# pinned superset of pyproject's core deps plus the dev/agentdojo/viz extras,
# for a one-shot `pip install -r requirements.txt`. It is not authoritative and
# may lag pyproject; when they disagree, pyproject wins.

agentdojo==0.1.35       # injection-eval benchmark: suites, attacks, defenses, deterministic checks
openai==2.44.0          # OpenAI-compatible client (also used for Groq via base_url)
python-dotenv==1.2.2    # load secrets from .env
pyyaml>=6.0             # config loader (threat_model.yaml)
pytest==9.1.1           # tests/
langgraph==1.2.6        # LangGraph adapter (tripwire/adapters/langgraph_adapter.py)
langchain-core==1.4.8   # message coercion helpers used by the LangGraph adapter
matplotlib>=3.8         # Pareto plot (scripts/experiments/pareto_plot.py); analysis-only, not imported by tripwire/

# Note: we run API models (Groq), so the heavy `agentdojo[transformers]` extra
# (pulls torch + transformers, ~2 GB) is intentionally omitted. Add it back only
# if you run LOCAL models:  pip install "agentdojo[transformers]"
