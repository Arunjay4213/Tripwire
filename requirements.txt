# tripwire — project dependencies (direct deps only)
#
# Regenerated 2026-06-29 from a clean Python 3.13 venv. The previous file was a
# full `pip freeze` of an anaconda environment: 360+ packages with unrelated
# tools (docling, yfinance, tensorflow, locust, ...) and a protobuf version
# conflict. These four are all tripwire actually needs; pip resolves the rest
# (~70 transitive packages, verified to install conflict-free).

agentdojo==0.1.35       # injection-eval benchmark: suites, attacks, defenses, deterministic checks
openai==2.44.0          # OpenAI-compatible client (also used for Groq via base_url)
python-dotenv==1.2.2    # load secrets from .env
pyyaml>=6.0             # config loader (threat_model.yaml)
pytest==9.1.1           # tests/

# Note: we run API models (Groq), so the heavy `agentdojo[transformers]` extra
# (pulls torch + transformers, ~2 GB) is intentionally omitted. Add it back only
# if you run LOCAL models:  pip install "agentdojo[transformers]"
