"""Test skeleton — real tests land Week 1+.

Keep one trivial placeholder so the suite is green and CI wiring is testable.
"""


def test_placeholder():
    # TODO Week 1: test the deterministic judge on hand-built traces,
    # adapter contract conformance, canary determinism, etc.
    assert True
